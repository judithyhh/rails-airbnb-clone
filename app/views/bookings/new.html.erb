<h1>New Booking</h1>
<% if current_user == @booking.lense.user %>
  <p>You can't book this lens -- You already own it!</p>
<% else %>
  <%= render 'layouts/add_booking_form', booking: @booking %>
<% end %>
<%= link_to 'Back', @lense%>
